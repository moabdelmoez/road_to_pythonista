fhand = open('mbox.txt')
count = 0

for line in fhand:
    """
    for each line in the file represented by the file handle,
    add one to the count variable.
    """
##    count = count + 1
    print(line)

##print("Line Count is:", count)
